{
  "name": "@mizan/preview",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Stage 2 preview. Renders every token in every mode combination, the live contrast ratio of every declared pairing, and the Market category screen twice — as v0 built it, and as the token system rebuilds it.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mizan/tokens": "*",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
